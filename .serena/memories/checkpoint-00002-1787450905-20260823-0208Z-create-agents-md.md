# Checkpoint: Create AGENTS.md Repository Guidance

- **Sequence**: 00002
- **Timestamp**: 1787450905-20260823-0208Z
- **Phase**: Project Setup & Guidance

## Summary of Accomplishments
1. Created `AGENTS.md` with concise, high-signal rules and commands for this MCP server repository.
2. Documented toolchain commands for `uv`, `pytest`, `ruff`, and `mypy`.
3. Outlined critical system invariants: state anti-tamper (HMAC), atomic lock handling, approval gates, phase verification rules, and DAG collision protection.

## Next Steps
1. Bootstrap `pyproject.toml` with MCP and Pydantic dependencies.
2. Implement core engine modules (`src/orchestrator_mcp/`).
3. Wire up `pytest-bdd` test harness for `docs/gherkin-scenarios.md`.
