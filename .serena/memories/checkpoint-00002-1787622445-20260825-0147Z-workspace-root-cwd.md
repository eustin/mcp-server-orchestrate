# Checkpoint 00002 — Resolve workspace to session cwd; drop workspace_root from tools

## State
- `resolve_workspace_root()` in `src/orchestrate_mcp/config.py` now returns `(start_path or Path.cwd()).resolve()` — no walking, no `.git`/`.opencode` markers, no cwd fallback (Serena lesson: cwd fallback burned them; opencode pins MCP server cwd to session dir).
- Verified empirically: `uv run --project <repo> python -c "print(os.getcwd())"` keeps caller cwd → opencode session folder.
- All 6 MCP tools (`orchestrate_init`, `_status`, `_approve`, `_verify`, `_archive`, `_get_dag_batches`) LOST the `workspace_root` param. Agents cannot override session dir. Removed unused `from pathlib import Path` in server.py.
- Internal helpers (config.py `resolve_workspace_root(start_path)`/`get_orchestrator_dir(workspace_root)`, state.py, lock.py, verifier.py) keep internal params — not MCP-exposed.
- opencode config (`~/.config/opencode/opencode.jsonc`): orchestrate MCP = `uv run --project /home/eusti/dev/orchestrate python -m orchestrate_mcp.server`, no `cwd` field → server cwd = opencode launch dir.

## Verification
- `uv run pytest`: 114 passed.
- `uv run ruff check src/ tests/`: clean.
- `uv run mypy src/ tests/`: clean (25 files).

## Test changes
- `tests/conftest.py` temp_workspace fixture now `monkeypatch.chdir(ws)` — cwd-based isolation.
- Stripped 61 `workspace_root=str(temp_workspace)` args across test_server.py (17+23) and test_bdd_scenarios.py (10+11).
- `tests/test_scaffolding.py`: test_config_resolution rewritten (cwd + chdir), added test_resolve_workspace_root_uses_given_path.

## Next steps
- Scrub remaining OpenCode-specific leaks: config.py clean now; still `server.py:214` docstring (`subagent` tool / hidden catalog), `execute.py:50-53` prompt (`subagent` tool + `general`/`explore` fallback).
- Update README + `.dev/*` docs: agent count 12 -> 8; workspace_root removal.
- Pre-existing EXECUTE prompt contradiction: `edit`/`write`/`serena_replace_content` in both forbidden + allowed (checkbox) lists.
